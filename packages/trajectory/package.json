{
  "name": "@hermests/trajectory",
  "version": "0.0.1",
  "description": "Trajectory compressor for HermesTS — post-processes completed agent trajectories to fit within a target token budget.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config": "./src/compression-config.ts",
    "./metrics": "./src/metrics.ts",
    "./compressor": "./src/trajectory-compressor.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@hermests/core": "workspace:*",
    "yaml": "^2.6.1"
  },
  "license": "MIT"
}
