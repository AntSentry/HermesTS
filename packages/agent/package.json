{
  "name": "@hermests/agent",
  "version": "0.0.1",
  "description": "Per-turn agent runtime for HermesTS — adapters, registries, auxiliary client, and conversation loop.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./auxiliary-client": "./src/auxiliary-client/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hermests/core": "workspace:*",
    "@hermests/providers": "workspace:*"
  },
  "license": "MIT"
}
