{
  "name": "@hermests/agent",
  "version": "0.0.1",
  "description": "Agent runtime for HermesTS — currently exposing the context-compression sub-task (sub-task #5k).",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./context-compression": "./src/context-compression/index.ts",
    "./context-compression/types": "./src/context-compression/types.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@hermests/core": "workspace:*",
    "@hermests/state": "workspace:*"
  },
  "devDependencies": {
    "@hermests/test-helpers": "workspace:*"
  },
  "license": "MIT"
}
